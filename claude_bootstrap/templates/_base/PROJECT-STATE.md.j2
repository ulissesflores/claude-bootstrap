{# Universal PROJECT-STATE.md template — curated, human-edited project state.
   Renamed from MEMORY.md to avoid a name collision with Claude auto-memory
   (~/.claude/projects/<path>/memory/MEMORY.md, v2.1.59+), which is a different,
   Claude-written file.
   Variables: project_name, generated_at
#}
# PROJECT-STATE.md — {{ project_name }}

> Curated, human-edited state for this project (decisions, goals, recent context). Update when state changes (new decision, completed milestone, abandoned approach). Distinct from Claude's auto-memory, which lives in `~/.claude/projects/<path>/memory/` and is written automatically.

---

## Project state

(empty — populate as work progresses)

---

## Active goals

(empty — what is in flight right now and why)

---

## Recent decisions

(empty — non-obvious choices, with the reasoning that motivated them)

---

## Known gotchas

(empty — surprising behaviors, fragile spots, things that bit us)

---

<!-- Generated by claude-bootstrap on {{ generated_at }} -->
